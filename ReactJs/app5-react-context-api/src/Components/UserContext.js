import { createContext } from "react";

export const PersonInfoContext = createContext();
export const PersonInfoContextProvider = PersonInfoContext.Provider;
export const PersonInfoContextConsumer = PersonInfoContext.Consumer;